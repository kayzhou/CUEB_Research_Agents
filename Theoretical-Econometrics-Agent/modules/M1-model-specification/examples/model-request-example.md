# 通用输入示例

请先运行 M1 `m1-model-specification`，模型获批后依次运行 M2 `m2-literature-positioning` 与 M3 `m3-qml-estimation`：

我想用高斯准极大似然研究线性条件均值模型
\(y_i=x_i'\beta_0+u_i\)，并允许 \(u_i\) 存在条件异方差。
请先写明抽样条件、参数空间、识别条件和准对数似然；模型获批后，再推导估计量、
三明治协方差矩阵及一致性与渐近正态性的证明蓝图。未经原文核验，不要提出新颖性结论。

